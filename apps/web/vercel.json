{
  "framework": "nuxtjs",
  "buildCommand": "pnpm run build",
  "outputDirectory": ".output/public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/__fallback" },
    { "src": "/admin", "dest": "/__fallback" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/__fallback" }
  ]
}
