{
  "name": "blueredandpurple",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit",
    "check": "oxlint && tsc --noEmit"
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
