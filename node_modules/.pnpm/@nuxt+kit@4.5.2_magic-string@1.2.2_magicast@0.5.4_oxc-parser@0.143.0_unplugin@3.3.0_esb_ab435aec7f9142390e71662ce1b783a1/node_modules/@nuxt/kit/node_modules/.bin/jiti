#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
else
  exec node  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/@nuxt+kit@4.5.2_magic-string@1.2.2_magicast@0.5.4_oxc-parser@0.143.0_unplugin@3.3.0_esb_ab435aec7f9142390e71662ce1b783a1/node_modules/jiti/lib/jiti-cli.mjs
