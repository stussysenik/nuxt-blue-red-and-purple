#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../terser/bin/terser" "$@"
else
  exec node  "$basedir/../../../terser/bin/terser" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/minimizer-webpack-plugin@5.6.1_esbuild@0.28.2_postcss@8.5.26_webpack@5.109.2_esbuild@0.28.2_postcss@8.5.26_/node_modules/terser/bin/terser
