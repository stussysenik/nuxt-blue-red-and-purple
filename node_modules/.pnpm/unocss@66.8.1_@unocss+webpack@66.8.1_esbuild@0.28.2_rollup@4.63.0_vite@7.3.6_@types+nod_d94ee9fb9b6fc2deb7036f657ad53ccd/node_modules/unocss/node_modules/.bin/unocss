#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@unocss/cli/bin/unocss.mjs" "$@"
else
  exec node  "$basedir/../../../@unocss/cli/bin/unocss.mjs" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/unocss@66.8.1_@unocss+webpack@66.8.1_esbuild@0.28.2_rollup@4.63.0_vite@7.3.6_@types+nod_d94ee9fb9b6fc2deb7036f657ad53ccd/node_modules/@unocss/cli/bin/unocss.mjs
