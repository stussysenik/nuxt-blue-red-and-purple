#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../acorn/bin/acorn" "$@"
else
  exec node  "$basedir/../../../acorn/bin/acorn" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/unimport@6.4.0_esbuild@0.28.2_oxc-parser@0.143.0_rollup@4.63.0_vite@7.3.6_@types+node@2_fa7603f95eade9af360f58eb91b308c5/node_modules/acorn/bin/acorn
