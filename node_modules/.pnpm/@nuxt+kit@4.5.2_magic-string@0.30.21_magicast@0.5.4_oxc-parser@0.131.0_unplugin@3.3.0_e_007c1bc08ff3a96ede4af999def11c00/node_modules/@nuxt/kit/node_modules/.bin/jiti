#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
else
  exec node  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/@nuxt+kit@4.5.2_magic-string@0.30.21_magicast@0.5.4_oxc-parser@0.131.0_unplugin@3.3.0_e_007c1bc08ff3a96ede4af999def11c00/node_modules/jiti/lib/jiti-cli.mjs
