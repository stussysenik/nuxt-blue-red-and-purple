#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/data/nuxt-blue-red-and-purple/node_modules/.pnpm/untyped@2.0.0/node_modules/untyped/node_modules:/data/nuxt-blue-red-and-purple/node_modules/.pnpm/untyped@2.0.0/node_modules:/data/nuxt-blue-red-and-purple/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/data/nuxt-blue-red-and-purple/node_modules/.pnpm/untyped@2.0.0/node_modules/untyped/node_modules:/data/nuxt-blue-red-and-purple/node_modules/.pnpm/untyped@2.0.0/node_modules:/data/nuxt-blue-red-and-purple/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../untyped/dist/cli.mjs" "$@"
else
  exec node  "$basedir/../untyped/dist/cli.mjs" "$@"
fi
# cmd-shim-target=/data/nuxt-blue-red-and-purple/node_modules/.pnpm/node_modules/untyped/dist/cli.mjs
